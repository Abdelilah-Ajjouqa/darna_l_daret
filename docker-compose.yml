version: '3.8'

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: darna_l_daret_dev
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development
    stdin_open: true
    tty: true
